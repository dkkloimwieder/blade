# Chrome WebGPU Profile

This directory contains a persistent Chrome profile for WebGPU development/testing.

Install WebGPU Inspector extension here for it to persist across sessions.

Launch Chrome with:
```bash
google-chrome --user-data-dir=/home/kaalin/dev/gfxx/blade/.chrome-profile --enable-unsafe-webgpu ...
```

